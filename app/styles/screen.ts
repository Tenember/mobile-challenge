import { StyleSheet } from "react-native";

export const AppStyle = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    justifyContent: "center",
    alignItems: "center",
  },
  pagePlaceholderText: {
    fontSize: 16,
    color: "#999",
  },
});
